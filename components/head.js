import React, { Component } from 'react'

import db from '../lib/db';

class Head extends Component {
    render() {
        return (
            <head>
                <title>Test</title>
            </head>
        )
    }
}

export default Head;